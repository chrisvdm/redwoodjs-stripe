module.exports = ({ subscription = false, seed=false}) => {
    console.log("Setting up Redwoodjs-stripe plugin...")
    
    /*
    STEPS
    =====
    1. Prompt for Stripe API Keys and save to .env
    2. If 'seed' = undefined ask to seed Stripe with test prices
    3. Create integration points
    4. Add boilerplate
    */
    // if (seed) console.log('Seeding Stripe Prices')
    // if(subscription) console.log("Setting up subscription payment flow")
}
